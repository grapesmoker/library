define(['backbone', 'jquery', 'underscore'],
  function(Backbone, $, _) {
    var Document = Backbone.Model.extend({
      initialize: function() {

      },
      defaults: {

      }
    });

    return Document
  }
);
